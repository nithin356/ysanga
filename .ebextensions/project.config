files:
  "/etc/php.d/project.ini" :
    mode: "000644"
    owner: root
    group: root
    content: |
      session.gc_maxlifetime = 3600000
      session.cookie_lifetime	 = 3600000
      upload_max_filesize = 100M
      post_max_size = 100M
      upload_tmp_dir = "/tmp"
